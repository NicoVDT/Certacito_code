# copy to .env and change everything before deploying anywhere real
SECRET_KEY=change-me-long-random-string
DATABASE_URL=postgresql+asyncpg://certacito:certacito@localhost:5432/certacito
# only set these to move the yaml somewhere else - the defaults sit next to
# backend/config.py and are right for a normal checkout
#POLICY_CONFIG=/path/to/policies.yaml
#RULE_LIBRARY=/path/to/rule_library.yaml
AGENT_API_KEY=change-me-agents-send-this-as-x-api-key
